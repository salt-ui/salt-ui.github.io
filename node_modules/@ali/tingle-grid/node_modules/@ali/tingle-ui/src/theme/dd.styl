/* variables + mixin + reset + flexbox + util */
@require "./../../node_modules/\@ali/tingle-style/src/tingle.styl";

$prefix = 't'
$brand-primary = #38adff; // D1_1
$brand-secondary = #efeff4; // D2_1

$font-size-field-text = $font-size-t6;
$font-size-field-label = $font-size-t6;
$font-size-field-title = $font-size-t4;

$font-size-field-text-lh = $font-size-t6-lh;
$font-size-field-label-lh = $font-size-t6-lh;
$font-size-field-title-lh = $font-size-t4-lh;

/* components style */
@require "./../components.styl";
