'use strict';

var path = require('path');
var R = require('ramda');
var loaderUtils = require('loader-utils');
var resolve = require('resolve');

function resolvePlugin(plugin) {
  var result = void 0;
  try {
    result = resolve.sync(plugin, {
      basedir: process.cwd()
    });
  } catch (e) {} // eslint-disable-line no-empty
  return result;
}

module.exports = function resolvePlugins(plugins, moduleName) {
  return plugins.map(function (plugin) {
    var snippets = plugin.split('?');
    var pluginName = path.join(snippets[0], 'lib', moduleName);
    var pluginQuery = loaderUtils.parseQuery(snippets[1] ? '?' + snippets[1] : '');
    var resolvedPlugin = resolvePlugin(pluginName);
    if (!resolvedPlugin) {
      return false;
    }
    return [resolvedPlugin, pluginQuery];
  }).filter(R.identity);
};